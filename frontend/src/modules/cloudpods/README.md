Phase 2: CloudPods module scaffold.

Refer to `docs/ENTERPRISE_PHASE2_SPEC.ix.md`.
- Add components, pages, api, hooks, and types as specified.
- Wire routes `/cloudpods` and `/cloudpods/:podId` without altering existing UI.
