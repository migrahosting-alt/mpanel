/**
 * extract-zip stub - placeholder until package is installed
 * This allows the server to start while the package installation issue is resolved
 */

export default async function extractZip(source, target) {
  throw new Error('extract-zip package not installed. Please run: npm install extract-zip');
}
